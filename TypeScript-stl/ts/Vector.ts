/// <reference path="API.ts" />

namespace std.Vector {

}

namespace std {
    /**
     *  Vector, dynamic array.
     *
     *  Vector are sequence containers representing arrays that can change in size.
     *
     *  Just like arrays. Vector use contiguous storage locations for their elements, which means that
     *  their elements can also be accessed using offsets on regular pointer to its elements, and jus as efficiently
     *  as in arrays. But unlike arrays, their size can change dynamically, with their storage being handled automatically by the
     *
     *
     */
}

